name: this is first yaml
on: 
   workflow_dispatch:
jobs:
     testing:
        name: Testing files
        runs-on: ubuntu-latest
        steps:
            - name: reading repo files using actons
              uses: actions/checkout@v4
            - name: listing files
              run: | 
                     echo "welcome to github actions"
                     ls -la
                     cat index.html